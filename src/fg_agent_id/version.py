"""Spec and package versions.

``DEFAULT_PROTOCOL_VERSION`` is the default value stamped into signed agent
cards; embedding protocols (e.g. AMP) may pass their own version instead.
"""

DEFAULT_PROTOCOL_VERSION = "0.1"
PACKAGE_VERSION = "0.1.0"
